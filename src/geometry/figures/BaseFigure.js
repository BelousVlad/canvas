export default class {
    constructor({x = 0, y = 0} = {}, drawler) {
        this.x = x;
        this. y = y;
        this.drawler = drawler;
    }

    isBelousPoint(point) {
        return false;
    }
}